import 'dart:math';  // from math import *
import 'dart:math' show sqrt;  // from math import sqrt
import 'dart:math' as math;  // import dart.math as math

main(List<String> args) {
  print(sqrt(11));
  print(math.sqrt(22));
}
